import React from 'react';
import ReactMarkdown from 'react-markdown';
import MarkdownToHTMLComponentStyles from './reactMarkdownComponents';

// Creates a card-based feed using markdown files. 
const CardFeed = ({ data }) => {
    // Return a set of HTML cards generated from markdown using ReactMarkdown. 
    return (
        // TODO: implement me :(
        <div/>
    );
};

export default CardFeed;